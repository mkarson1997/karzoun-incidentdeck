analyzer:
  exclude:
    - build/**

linter:
  rules:
    - always_declare_return_types
    - avoid_print
    - directives_ordering
    - prefer_final_fields
    - prefer_final_locals
    - use_super_parameters
